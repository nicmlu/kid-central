<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Homepage</title>
</head>
<body>
<div class="logout-button">
  <form action="/logout">
  <input type="submit" value="LOG OUT">
  </form>
  <br>
</div>
  <p><h1 class="welcome">Welcome, <%= @user.name%>!</h1></p>
  <div class="row">
  <div class="column-left">
  <p><h3>Here are the <%= @user.family_name%> children:</h3></p>
  <ol>
  <% @user.kids.each do |kid| %>
    <li><a href="/kids/<%=kid.id%>"><%=kid.name%></a></li>
   <% if @user.kids == "nil"%>
    <%= "You don't have any kids saved yet." %>
     <% end %>
  <% end %>
</ol>
<br>
  <form action="/kids/new">
  <input type="submit" value="Add a Kid Profile">
  </form>
  <br>
  <form action="/kids">
  <input type="submit" value="All Kid Profiles">
  </form>
  </div>
<div class="column-right">
<p><h3>Here are your children's upcoming events:</h3></p>
  <ol>
  <% @events.each do |event| %>
<li><a href="/events/<%=event.id%>"><%=event.name%> - <%=event.date.strftime('%a, %b %d %Y')%></a></li>       <% if @user.events == "nil"%>
    <%= "You don't have any events saved yet." %>
     <% end %>
  <% end %>
</ol>
<br>
  <form action="/events/new">
  <input type="submit" value="Add an Event">
  </form>
  <br>
  <form action="/events">
  <input type="submit" value="All Events">
  </form>
<br>
</div>
</div>

</body>
</html>