<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title></title>
</head>
<body>
<div class="logout-button">
<p><a href="/logout">LOG OUT</a></p>
</div>

  <p><h1><%= current_user.family_name%> Family Events:</h1></p>
    <% if !current_user.events%>
    <%= "You don't have any events saved yet." %>
    <% else %>
  <% current_user.events.each do |event| %>
  <ol>
    <li><a href="/events/<%=event.id%>"><%=event.name%> - <%=event.date.strftime('%a, %b %d %Y')%></a></li>
    <% end %>
    <br>
    <br>
      <form action="/events/new">
        <input type="submit" value="Add an Event">
      </form>
    <br>
      <form action="/homepage">
         <input type="submit" value="Back to Kid Central Homepage">
      </form>
<br>
<br>
<br>
</body>
</html>